export enum SLASH_COMMANDS {
  ECHO = "/echo",
  TEAM_QUEUE = "/team-queue",
  MY_QUEUE = "/my-queue",
  GET_QUEUE = "/get-queue",
  FIXED_PR = "/fixed-pr",
  REQUEST_MORE_CHANGES = "/request-more-changes",
}